# MiLens 提醒与通知文案设计

最后更新：2026-08-12

本文是 MiLens iOS 的提醒、通知、页内提示文案母稿。源语言为简体中文；后续本地化以本文的语义、触发条件和占位符为准，不直接逐字翻译代码中的临时文案。

## 1. 文案总原则

- **像一位温柔的记录者**：提醒用户有一段回忆可以看看，不制造焦虑，也不要求用户立刻完成任务。
- **先说发生了什么，再说可以做什么**：标题负责识别场景，正文负责给出轻量动作。
- **不替用户编故事**：只使用真实的日期、宠物名称、用户备注和已存在的照片；不把通用识别结果写成确定的宠物语义。
- **不夸大隐私承诺**：通知只呈现必要信息；不写“绝不丢失”“永远不会离开设备”等绝对化承诺。
- **尊重通知频率**：同一场景只保留一条主提醒；没有有效照片或没有有效日期时不发通知。
- **产品术语统一**：宠物档案、往日回忆、时光机、纪念提醒、成为家人的日子、拼豆图纸。
- **空格规则**：简体中文、繁体中文、日文、韩文版{petName}前后、数字和拉丁字母前后一律不加空格。
- **应用名称规则**：提及本应用名称时，简体中文、繁体中文版一律使用「咪Lens」；西洋语言一律使用 MiLens（无引号）；韩文、日文版一律使用「MiLens」。

### 长度建议

| 位置 | 建议长度 | 写法 |
|---|---:|---|
| 系统通知标题 | 6–14 个汉字 | 先让用户知道是哪一类回忆 |
| 系统通知正文 | 12–32 个汉字 | 一句话为主，最多两句 |
| Toast / banner | 8–20 个汉字 | 说结果，不解释实现细节 |
| Alert 标题 | 6–12 个汉字 | 直白、可理解 |
| Alert 正文 | 1–2 句 | 说明原因和下一步 |

## 2. 当前已实现的系统通知

当前 `NotifyService` 实际调度四类通知：宠物生日/成为家人的日子年度提醒、相处里程碑单次提醒，以及有历史同日照片时的时光机提醒。通知时间为当天 09:00；以下文案可以直接作为现有 `notify.*` key 的内容基线。

### 2.1 生日提醒

触发：宠物档案填写了生日；每年生日当天 09:00。

推荐版本：

| 字段 | 文案 |
|---|---|
| 标题 | `今天是{petName}的生日` |
| 正文 | `去看看{petName}的生日回忆吧。` |

实现已将宠物名称和事件类型提升到标题，锁屏上可以直接识别场景。

### 2.2 成为家人的日子提醒

触发：宠物档案填写了成为家人的日期（内部字段仍为 `adoptionDay`）；每年当天 09:00。

推荐版本：

| 字段 | 文案 |
|---|---|
| 标题 | `今天是和{petName}成为家人的日子` |
| 正文 | `去看看你们一起留下的回忆吧。` |

说明：标题不写“领养成功”或“命中注定”，也不使用“领养纪念日”，以覆盖真实经历的差异。

### 2.3 时光机提醒

触发：未来 7 天内某一天存在历史同日照片；当天 09:00 单次发送。

推荐固定标题：

`{yearsAgo}年前的今天`

推荐正文模板，按照片是否有备注和是否有关联宠物选择：

| 场景 | 推荐文案 |
|---|---|
| 有宠物名 | `{yearsAgo}年前的这一天，{petName}也陪在你身边。` |
| 有宠物名、适合提问 | `{yearsAgo}年前的今天，{petName}在做什么呢？` |
| 无宠物名 | `时光飞逝，这一天居然已经过去{yearsAgo}年了。` |
| 有备注 | `还记得{yearsAgo}年前的今天吗？{note}` |

当前实现保留三个模板槽位：`asking`、`companion`、`flight/flightNote`。`flightNote` 是有备注时的内容分支，不单独占用模板槽位；`flashback` 已移出实际模板池。

空数据规则：没有历史同日照片时不调度；没有备注时不保留冒号；无法关联宠物时使用“这段回忆”或“小宝贝”，不猜测宠物名称。

## 3. 通知权限相关文案

### 3.1 设置页说明

设置项保留：

| 位置 | 文案 |
|---|---|
| 分组标题 | `通知` |
| 开关名称 | `纪念提醒` |
| 说明 | `开启后，生日、成为家人的日子、相处里程碑和时光机会在合适的时候提醒你。` |

说明不必把“每天 09:00”和“未来 7 天”写得过于技术化；如果需要明确行为，可用辅助说明：

`生日和成为家人的日子当天 09:00 提醒；里程碑和时光机在合适的时候提醒。`

当前 `settings.notifications.footer` 的行为描述是准确的，可以作为开发/测试版本；面向普通用户时建议采用上面的短版。

### 3.2 请求系统权限前的页内说明

标题：`别错过重要回忆`

正文：`允许「咪Lens」发送通知，我们会在生日、成为家人的日子和有新回忆可看的时候提醒你。你可以随时在系统设置中关闭。`

按钮：`允许提醒` / `暂不允许`

说明：这是产品自己的说明，不替代 iOS 系统权限弹窗；不应写成“打开通知才能使用「咪Lens」”。

### 3.3 权限被拒绝

当前 key：`settings.notifications.denied.title/message/ok`

推荐版本：

| 字段 | 文案 |
|---|---|
| 标题 | `通知权限未开启` |
| 正文 | `如需接收纪念提醒，请在系统设置中允许「咪Lens」发送通知。` |
| 按钮 | `知道了` |

如果页面提供跳转系统设置，按钮改为：`去系统设置`，并保留一个次级按钮 `稍后再说`。不要在用户拒绝后立即重复弹窗。

## 4. 页内提醒、Toast 与 Banner

这些不是系统推送，重点是让用户知道操作结果；不使用感叹号堆叠，不把成功提示写成营销口号。

| 触发场景 | 推荐文案 | 类型 |
|---|---|---|
| 扫描完成，有发现 | `发现{count}张值得整理的照片` | 完成提示 |
| 扫描完成，无发现 | `这次没有发现新的照片` | 空结果 |
| 导入完成 | `已加入{count}张照片` | 完成提示 |
| 重复照片跳过 | `已跳过{count}张重复照片` | 补充提示 |
| 保存宠物档案 | `档案已保存` | Toast |
| 保存编辑照片 | `照片已保存到档案` | Toast |
| 生成拼豆图纸完成 | `图纸做好了` | 完成提示 |
| 导出完成 | `已准备好分享` | 完成提示 |
| 分享取消 | `已取消分享` | Toast，可选 |
| 删除前确认 | `删除这份档案？` / `相关照片记录也会从 MiLens 中移除，系统相册中的原照片不受影响。` | Alert |
| 清理缓存完成 | `缓存已清理，照片和档案未受影响` | 完成提示 |
| 离线备份完成 | `备份已准备好，请选择保存位置` | 完成提示 |
| 恢复完成 | `已恢复{count}条记录` | 完成提示 |

数量为 1 时使用“1 张/1 条”；数量大于 1 时使用对应复数形式。数量为 0 时不要显示“已完成”，改用空结果文案。

## 5. 错误与降级提示

错误文案按“三段式”写：发生了什么 → 用户数据是否安全 → 下一步做什么。

| 场景 | 推荐文案 |
|---|---|
| 照片权限不足 | `无法读取照片。请在系统设置中允许「咪Lens」访问照片，然后重试。` |
| 扫描失败 | `这次扫描没有完成，已有档案不会受影响。请稍后重试。` |
| 单张分析失败 | `有一张照片暂时无法分析，其他照片仍可继续处理。` |
| 图纸生成失败 | `图纸没有生成成功，原照片未受影响。请换一张照片再试。` |
| 导出失败 | `暂时无法导出这份作品，原内容已保留。请稍后重试。` |
| 备份文件损坏 | `这个备份文件无法读取，现有档案不会受影响。请换一个备份文件重试。` |
| 恢复部分跳过 | `已恢复新记录，已有同 ID 的记录已保留。` |
| 数据库启动恢复 | `「咪Lens」暂时无法打开本地档案。可以先导出诊断信息，或重建本地数据。` |

不要把内部错误码、路径、模型名称或“SwiftData/Core ML”直接展示给普通用户；这些内容进入诊断日志。

## 6. 已接入与规划中的提醒

相处里程碑已接入 `NotifyService`：按宠物 `adoptionDay` 计算 100/365/730/1000 天，使用稳定 ID 单次调度，并随宠物编辑/删除同步撤销。其他触点仍为规划项。

| 触点 | 触发条件 | 建议文案 |
|---|---|---|
| 相处里程碑 | 第 100/365/730/1000 天 | 标题：`来到家{days}天`；正文：`{petName}已经来到这个家{days}天了`（已接入） |
| 照片数量里程碑 | 达到 50/100/500 张 | 标题：`你和{petName}的第{count}张照片`；正文：`又多了一页可以回看的日常。` |
| 月度回忆 | 月初生成精选 | 标题：`{month}月的回忆准备好了`；正文：`看看这个月最值得留下的片段。` |
| 年度回忆 | 年末或用户主动生成 | 标题：`{year}年的回忆`；正文：`这一年，你们一起留下了{count}段回忆。` |
| 长期未打开 | 后续如产品确认需要 | 暂不建议默认启用；避免用“你已经很久没来看了”制造负担。 |

里程碑通知的标题应以陪伴和事实为主，不使用“快来”“别错过”“最后机会”等催促词；照片数量里程碑也不要暗示照片越多越好。

## 7. 不采用的表达

| 不建议 | 原因 | 推荐替代 |
|---|---|---|
| `你有一条重要通知` | 信息量不足，像系统告警 | `今天是{petName}的生日` |
| `快来看看！` | 催促、缺少场景 | `去看看这段回忆吧。` |
| `AI 发现了你和宠物的故事` | 超出当前能力事实 | `发现一张可以整理的照片` |
| `照片永远不会丢失` | 绝对化承诺，忽略备份边界 | `档案已保存到「咪Lens」` |
| `回忆杀` | 网络化且可能触及敏感纪念场景 | `往日回忆` / `这段回忆` |
| `你已经 X 天没打开了` | 产生被监控感 | 默认不发此类通知 |

## 8. 落地顺序

1. 先锁定系统通知和权限拒绝文案，再保持调度 ID 与空数据规则稳定。
2. 再补充固定 locale 快照测试，至少覆盖简体中文、英文、德语和日语；重点检查宠物长名、备注为空、`1 年/2 年` 和通知换行。
3. 需要增加新通知类型时，先补“触发条件、去重 ID、关闭入口、空数据规则”，再新增本地化 key。
4. 里程碑已进入 `Localizable.xcstrings` 并接入调度；照片数量、月度/年度回忆仍属于规划文案。

## 9. 代码现状与整合结果清单

本节盘点当前代码中的文案变体、模板和同义表达，并记录本次整合后的实现状态。资源键仍以代码调用为准；未再被调用的旧键后续可在本地化资源清理阶段删除。

### 9.1 系统通知：当前实际调度

| 编号 | 代码位置 / key | 触发与分支 | 当前简体中文输出 | 状态 |
|---|---|---|---|---|
| N01 | `notify.anniversary.birthday.title %@` | 生日年度提醒标题 | `今天是%@的生日` | 已实装；按宠物区分 |
| N02 | `notify.anniversary.birthday.body %@` | 生日年度提醒正文 | `去看看%@的生日回忆吧。` | 已实装 |
| N03 | `notify.anniversary.adoption.title %@` | 成为家人的日子标题 | `今天是和%@成为家人的日子` | 已实装 |
| N04 | `notify.anniversary.adoption.body %@` | 成为家人的日子正文 | `去看看你们一起留下的回忆吧。` | 已实装 |
| N05 | `notify.memory.title` | 历史照片回忆通知标题 | `往日回忆` | 已实装；与年度提醒拆分 |
| N06 | `notify.memory.today %@` | 年份差为 0 的纪念照片正文 | `往年今日的回忆：%@` | 已实装 |
| N07 | `notify.memory.yearsAgo %lld %@` | 年份差大于 0 的纪念照片正文 | `%lld年前的今天：%@` | 已实装；存在复数变体 |
| N08 | `notify.timemachine.title %lld` | 时光机通知标题 | `%lld年前的今天` | 已实装 |
| N09 | `notify.timemachine.asking %lld %@` | 时光机模板 0 | `%lld年前的今天，%@在做什么呢？` | 已实装，模板池 |
| N10 | `notify.timemachine.companion %lld %@` | 时光机模板 1 | `%lld年前的今天，%@这样陪伴着你` | 已实装，模板池 |
| N11 | `notify.timemachine.flight %lld` | 时光机模板 2，无备注 | `时光飞逝，%lld年前的今天` | 已实装，模板池 |
| N12 | `notify.timemachine.flightNote %lld %@` | 时光机模板 2，有备注 | `%lld年前的今天，%@` | 已实装，模板 2 的备注分支 |
| N13 | `notify.defaultPetName` | 无法关联宠物时的回退名称 | `小宝贝` | 已实装 |
| N14 | `notify.milestone.title %lld` | 相处里程碑标题 | `来到家%lld天` | 已实装，单次通知 |
| N15 | `notify.milestone.body %@ %lld` | 相处里程碑正文 | `%@已经来到这个家%lld天了` | 已实装，单次通知 |

当前真实组合关系：生日和成为家人的日子分别调用 `buildPetAnniversaryNotification` 生成标题/正文；历史照片回忆使用 `notify.memory.*`，不再复用年度提醒标题。

### 9.2 首页回忆与临近日子

| 编号 | 代码位置 / key | 触发与分支 | 当前简体中文输出 | 状态 |
|---|---|---|---|---|
| H01 | `HomeMemoryLogic` + `HomeViewModel` | 有历史同日照片 | `{yearsAgo}年前的今天` | 已由 App 注入本地化标题 |
| H02 | `home.memoryTitle` + `HomeViewModel` | 没有同日照片，回退最近历史照片 | `最近回忆` | 已由 App 注入本地化标题 |
| H03 | `home.upcoming.birthday %@` | 首页下一项是生日 | `%@的生日` | 当前首页实际显示 |
| H04 | `home.upcoming.adoption %@` | 首页下一项是成为家人的日子 | `第一次见面的日子` | 当前首页实际显示；保留为首页短标签 |
| H05 | `home.upcoming.days.*` | 首页临近日子辅助信息 | `还有%lld天·已陪伴%lld天` 等分段文案 | 已按 180/365/366 天分段整合 |
| H06 | `home.upcoming.lookBack` | 点击临近日子卡片 | `回看那一天 →` | 当前首页实际显示 |
| H07 | `home.memoryOpen` | 点击回忆卡片 | `查看这段回忆` | 当前首页实际显示 |

关联的纯逻辑默认值仍保留用于 Kit 独立调用，但 App 已注入本地化日期与无空格分隔符；它们不是通知模板，却会和时光机/首页回忆一起出现。

### 9.3 纪念卡、档案和相处天数

| 编号 | 代码位置 / key | 触发与分支 | 当前简体中文输出 | 状态 |
|---|---|---|---|---|
| C01 | `pet.card.daysHome %lld` | 兼容的纪念卡日期行 | `来到家%lld天` | 保留资源；档案展示改用分段陪伴文案 |
| C02 | `pet.card.birthdayYears %lld` | 生日纪念卡日期行 | `%lld岁` | 当前卡片实际使用；外层标题另有生日表达 |
| C03 | `PetDisplayLogic.companionshipText` | 宠物列表相处天数 | `来到家N天` / `相处N天` / `和{petName}成为家人N天` | 已按天数分段整合 |
| C04 | `MemoryCardKind.displayName` | 卡片类型入口/调试及预留显示 | `生日纪念` / `成为家人纪念` / `里程碑` / `成长对比` / `月度精选` / `年度回忆` | Kit 硬编码；部分类型仍为规划能力 |
| C05 | `pet.card.fallbackTitle` | 无宠物或无日期的卡片标题 | `这一天` | 当前逻辑可达 |
| C06 | `pet.card.fallbackSubtitle` | 无宠物或无日期的卡片副标题 | `值得记住的一天` | 当前逻辑可达 |

这里保留 `pet.card.daysHome` 作为兼容资源；面向用户的档案列表和卡片日期行统一走 `PetDisplayLogic.companionshipText`，按时长分段表达“来到家 / 相处 / 成为家人”。

### 9.4 时间线与成长提醒

| 编号 | 代码位置 / key | 触发与分支 | 当前简体中文输出 | 状态 |
|---|---|---|---|---|
| T01 | `timeline.birthday.title %@ %lld` | 时间线推导生日事件 | `%@%lld岁啦！🎂` | 当前时间线实际显示；偏庆祝语气 |
| T02 | `timeline.birthday.subtitle %lld` | 时间线生日副标题 | `第%lld个生日` | 当前时间线实际显示 |
| T03 | `timeline.type.birthday` | 时间线类型标签 | `生日` | 当前时间线实际显示 |
| T04 | `timeline.type.adoption` | 时间线成为家人的日子类型标签 | `纪念日` | 当前时间线实际显示；短标签保留 |
| T05 | `TimelineLogic` 事件标题回退 | 系统事件无标题 | `生日` / `和{petName}成为家人的日子` | 已移除硬编码回退 |
| T06 | `TimelineLogic` 参考照片提醒 | 幼宠且已注册特征，生成时间线提醒 | `建议为{petName}更新参考照片` | 已本地化；不是系统通知 |
| T07 | `TimelineLogic` 参考照片提醒副标题 | 同上 | `{petName}正在快速成长，建议重新拍摄注册` | 已本地化；保留现有产品动作 |

### 9.5 里程碑、月度/年度回忆

| 编号 | 代码位置 | 触发与分支 | 当前简体中文输出 | 状态 |
|---|---|---|---|---|
| P01 | `MilestoneLogic.milestoneTitle` / `notify.milestone.title` | 100/365/730/1000 天里程碑标题 | `来到家{days}天` | 已接入 `NotifyService` |
| P02 | `MilestoneLogic.milestoneNotificationText` / `notify.milestone.body` | 里程碑通知正文 | `{petName}已经来到这个家{days}天了` | 已接入 `NotifyService` |
| P03 | `MemoryRecapLogic.monthlyTitle` | 月度精选标题 | `{year}年{month}月` | 纯逻辑已存在；当前未进入本地化资源 |
| P04 | `MemoryRecapLogic.yearlyTitle` | 年度回忆册标题 | `{year}年度回忆` | 纯逻辑已存在；当前未进入本地化资源 |
| P05 | `MemoryCardKind.displayName` | 月度/年度卡片类型名 | `月度精选` / `年度回忆` | 纯逻辑已存在；部分 UI 入口仍属预留 |

### 9.6 当前相关硬编码文案（非通知，但会在同一语义链出现）

以下文案已在本次整合中完成本地化或仍需后续处理：

| 代码位置 | 当前文案 | 备注 |
|---|---|---|
| `PetsView` | `为他/她建立一份档案，\n照片、纪念日和故事都会留在这里。` | 已改为 `pets.empty.body` |
| `CreateView` | `把一张照片做成竖版纪念卡，带上名字与成为家人的日子。` | 已使用现有本地化 key |
| `PetCardPhotoPickerView` | `选择一张照片生成纪念卡片` | 已改为 `create.petCard.selectPhoto` |
| `PetCardView` | `长按或保存到相册后，可作为壁纸或分享给家人。` | 已使用本地化 key |
| `GrowthCompareView` | `两张照片，一段时光。保存到相册或分享给家人。` | 已使用本地化 key |
| `TimelineView` | `还没有成长记录` / `添加伙伴档案和照片后，这里会自动生成成长时间线` | 已改为 `timeline.empty.*` |
| `BeadPresetResolver` / `BeadSettingsPanelView` | `保留照片背景，适合有纪念意义的画面` | 拼豆预设说明，不属于提醒系统 |

这些条目已完成本次范围内的本地化与术语收敛；拼豆预设说明属于独立能力，暂不纳入通知文案体系。

## 10. 已确认并实装的决策

1. **生日/成为家人的日子通知结构**：标题直接包含 `{petName}` 与事件类型。
2. **成为家人的日子主术语**：系统通知使用 `今天是和{petName}成为家人的日子`；内部字段继续使用 `adoptionDay`。
3. **时光机模板池**：保留三个槽位：提问、陪伴、备注/无备注；N11“回忆杀”不再进入模板池。
4. **默认宠物名**：无关联宠物时使用 `小宝贝`。
5. **陪伴天数术语**：`来到家N天`（0–180 天）、`相处N天`（181–365 天）、`和{petName}成为家人N天`（366 天及以上）；未来日期与缺失日期均有回退文案。
6. **时间线生日语气**：保留 `%@%lld岁啦！🎂` 的庆祝感。
7. **规划能力范围**：P01/P02 相处里程碑已接入系统通知；P03–P05 仍保留为后续卡片/回忆能力。
8. **硬编码清理范围**：已处理 9.6 中的创作入口、档案空态和时间线空态；拼豆预设说明仍不属于提醒文案范围。

整合已完成：`Localizable.xcstrings`、纯逻辑、通知组装、调用页面、行为测试和本文档已同步；本地化检查作为最终校验。
